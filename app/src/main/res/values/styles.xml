<resources>

    <!-- Base application theme. -->
    <!-- LIGHT THEME. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">#fff</item>
        <item name="textColor">#000</item>
        <item name="textColorDim">#2C2C2C</item>
        <item name="googleBacground">#d44a3d</item>
        <item name="textBlueColor">#4282ef</item>

    </style>

    <style name="DrawerIconLight" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">#292929</item>
    </style>

    <!-- DARK THEME theme -->

    <style name="DrawerIconDark" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">#FFFFFF</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundColor">#000</item>
        <item name="textColor">#fff</item>
        <item name="textColorDim">#A0A0A0</item>
        <item name="googleBacground">#d44a3d</item>
        <item name="textBlueColor">#4282ef</item>
    </style>



    <style name="FullScreenDialogStyle" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- Set this to true if you want Full Screen without status bar -->
        <item name="android:windowFullscreen">false</item>

        <item name="android:windowIsFloating">false</item>

        <!-- This is important! Don't forget to set window background -->
        <item name="android:windowBackground">#9C3D3D3D</item>

        <!-- Additionally if you want animations when dialog opening -->
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

</resources>
